/**
 * Presentation Layer (Controllers).
 * Exposes RESTful APIs and serves web endpoints for clients,
 * handling incoming HTTP requests, delegating to services, and returning responses.
 */
package com.smartcurriculum.portal.controller;
